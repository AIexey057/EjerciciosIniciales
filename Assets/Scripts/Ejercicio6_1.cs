using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio6_1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Entiendo que este ejercicio se hara de foema escrita
        // a = ya que pones las || significa que puede ser una u otra asique la primera sera false y la segunda sera true
        //c = igual que la anterior en este caso la primera es true y la segunda false
        //e = true; false; true;
        //b = true
        //d = false
        //f = true
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
